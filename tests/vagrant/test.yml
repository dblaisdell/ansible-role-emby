---

- name: Test emby role
  hosts: all
  remote_user: root

  pre_tasks:
    - name: Update apt
      become: yes
      apt:
        cache_valid_time: 1800
        update_cache: yes
      tags:
        - build

  roles:
    - role: rofrantz.emby
      tags:
        - always

#  post_tasks:
#    - name: Nginx should be installed
#      command: nginx -v
#      tags:
#        - assert
